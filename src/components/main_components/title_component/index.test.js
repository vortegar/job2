import { render, screen } from '@testing-library/react';
import TitleComponent from './index';

describe('Datagrid_component testing', () => {

    test('Title Text', () => {
        render(<TitleComponent></TitleComponent>);
        
    })
  


})