import { render, screen } from '@testing-library/react';
import LockableButtonComponent from './index';
import DataGridComponent from './index';


describe('Lockable Button testing', () => {

    test('Title Text', () => {
        render(<LockableButtonComponent></LockableButtonComponent>);
        
    })
  


})